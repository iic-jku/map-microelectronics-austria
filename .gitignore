# Build artifacts
microelectronics_austria.svg
_site/

# Note: microelectronics_austria.png is committed by the CI workflow
# (rendered from the SVG on every push), so it is intentionally tracked.

# Python
__pycache__/
*.py[cod]

# macOS
.DS_Store

